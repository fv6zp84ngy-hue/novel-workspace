# 0.4.1 更新说明

本包是面向 GitHub Release 的完整使用版，包含程序、预构建浏览器模块、启动入口与文档。新版发布后，由 Latest Release 的同名下载资源接替旧的默认下载入口。主要迭代流程图和验证效果已展示在 [README](README.md)。此次未修改稿件数据库结构。

## 更新了什么、效果如何

| 更新 | 旧问题 | 本次行为 | 已验证的效果 |
| --- | --- | --- | --- |
| 首次价值判定 | 系统自动存入“创作起点”也被算作作者激活 | 新故事出现产物后，作者主动保存标题/正文或资料才计入；迁移后需找回/关联资料 | 合成 UI：只创建就退出不激活，只改标题并保存会激活 |
| 深度交互判定 | 自动动作可能凑够三种行为 | 只计主动编辑、添加资料、打开搜索结果、关联章节；三类才达标 | 自动、失败、未知来源动作均被排除；重复保存不增加类别 |
| 开放表达分析 | 混入迁移会话，选中入口可能被误当提交 | 仅以新故事会话为分母，区分自然语言、模板、空白和未提交 | 可分别比较 control / treatment 的使用结构，尚无真人提升结论 |
| 场景入口分析 | 不知道哪些卡片被看见、点击并带来后续价值 | 可见曝光 → 选择 → 意图提交 → 首次价值；按会话去重并固定归因 | 折叠卡片不记曝光；重复导出不放大样本，提交前最后选择场景获得归因 |
| 数据可比性 | 旧口径、重复导出、多次开始可能混在一起 | 隔离旧版本，重新进入开始页建立新会话，按事件 ID 去重 | 旧版自动激活不会混入新口径，刷新不会重复记录派生事件 |
| 快速输入 | 点击卡片后等待埋点再重绘，可能覆盖刚输入的文字 | 先更新界面，再写入点击埋点 | 两组实际浏览器链路回归通过 |
| 真人验证准备 | 缺少统一任务和证据模板 | 提供两类作者的任务脚本、访谈、空白记录表、四份虚构 TXT/Markdown | 流程已备好；真人完成 0 / 至少 5 人，研究仍待开展 |
| 使用与升级交付 | 下载者需要找启动命令，版本目录不便覆盖 | GitHub Latest 同名完整发布包，macOS / Windows 启动文件，启动后开浏览器，原端口可沿用 | 不需要 npm 安装或构建；端口占用时不偷偷换地址 |

## 怎样理解“效果”

**已确认的是行为正确性与测量可信度改善。** 自动行为不再被误当作作者激活，能够更准确观察首次使用的阻塞点。修正口径后，统计的完成率可能下降，这不等于用户体验退步，而是去除了原先的高估。

**尚未证明的是增长幅度。** 没有足够真人样本，不能说 TTFV 已缩短多少、转化提升多少，不能将历史业务“+12%”套到当前原型。真人研究完成前，适合描述为“实现首次交付链路与本地行为测量，修正激活口径，建立可复现验收及用户验证流程”。

具体测试环境、通过项和边界见 [测试报告](docs/TEST_REPORT.md)；真人执行见 [任务手册](docs/REAL_USER_VALIDATION.md)。Mac 无头浏览器测试不能替代 Windows 真机或目标作者研究。

## 保留的能力

正文、大纲、资料分类、关键词搜索、章节关联、本机加密、历史版本、备份与恢复，以及已有用户自带密钥的模型功能与 WebDAV 能力都包含在包内。本次未扩展 AI、RAG、账号或云分析。真实付费模型、真实网盘兼容性及多设备极端故障仍遵循既有待验边界。

## 发布与隐私

本包按文件白名单构建，不含浏览器稿件库、用户凭据、研究原始记录、Git 历史或开发者本机路径。不要把你的备份或研究导出复制进公开仓库。随包小说素材与浏览器测试内容均为虚构样例。
