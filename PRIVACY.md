# 隐私说明 · 0.3.0

没有平台账号、第三方统计脚本或操作事件采集。默认离线写作；模型和 WebDAV 都是用户主动配置、点击后使用。

| 数据 | 处理位置 | 生命周期与外发 |
| --- | --- | --- |
| 稿件、历史、来源、索引、任务、引用 | 浏览器内存与加密 IndexedDB | 解锁期间内存为明文；清理网站数据或浏览器驱逐可能丢失 |
| 资料库密码 / 派生密钥 | 当前网页内存 | 刷新/锁定后不可继续读取；不发给服务商，不写入备份字段 |
| 模型 API Key | 输入框后转入本机 Python 内存 | 输入后清空字段；会话闲置一小时失效并定时清理；主动清除或停止进程可移除；不入库不写日志 |
| 模型输入、回答、向量 | 本机网关与用户选定的模型服务商 | 仅点击后发送；服务商的留存、训练与收费遵循其政策 |
| WebDAV 用户名和应用密码 | 输入框、本机请求内存 | 不写库或备份；关闭面板移除输入，成功上传后清空密码字段 |
| 云快照与协作文件 | 用户选择的 WebDAV 网盘 | 正文与合并更新均在密文内；网盘可见地址、大小、时间等元数据 |
| 导出文件 | 用户下载目录 | 完整备份加密，TXT 为明文；用户自行管理 |

RAG 发送问题与最多五个候选片段；建立向量时每次最多十六个片段。分类发送标题与前四千字，关联检索发送章节标题与前一千五百字，草稿生成发送当前创作要求。服务商会看到这些明文；请只选择自己信任的服务。

回收站是软删除。历史清理不是磁盘安全擦除，旧版本可能在已有备份和磁盘残留中。密码遗失无法恢复。恶意扩展、同源脚本或设备被控制时，解锁数据仍有风险。共享电脑请离开时锁定，并清除服务凭据。

源码发布不包含浏览器数据库。不要把稿件、备份、密钥、网盘地址中的私人标识提交到公开 Issue。静态托管方仍会处理页面访问请求；不同路径的同源网站不是隔离空间。

章节协作是用户打开协作编辑并连接网盘后才启动的自动轮询。普通写作不会自动联网。协作口令和 WebDAV 应用密码仅保留在当前协作窗口内存，不进入 IndexedDB 或整库备份；本地 CRDT 更新则和正文一起写入加密资料库。关闭窗口停止轮询。
